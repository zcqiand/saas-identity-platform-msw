# .env.test — vitest 测试期覆盖（与 .env.example 同源 dev key）
#
# vitest mode=test 时加载（优先级高于 .env.example）。
# HS256 真签发需要 ≥32 字节 key，与 .env.example 一致保证测试/MSW/dev 三路一致。

JWT_SIGNING_KEY=dev-key-32-bytes-minimum-length!
JWT_ISSUER=saas-identity-platform
JWT_AUDIENCE=saas-identity-platform-clients
JWT_TTL_SECONDS=3600
node_modules/
generated/
.state/
coverage/
*.log

# vitest/esbuild transform 遗留（package.json "type":"module" 下 ESM 失败时
# esbuild 会把 src/**/*.ts 转成 src/**/*.js 留在仓里；本仓 library + dev server
# 不发 JS 产物，这些 .js 全是 transform 垃圾）
src/**/*.js

# Claude 临时文件
.claude/
.superpowers/

# .env.production: 部署契约真值(含 secret),gitignored 不进 git(2026-08-28 起,同 .env.local 模式)
# key 集合由 suite L0.5 门核对(存在时与 .env.example 严格双向比对);真值经 scripts/sync-secrets 进 GitHub Secrets
.env.production
